<%= button_to 'logout', destroy_user_session_path, method: :delete  %>

<% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %>

<% @gifs.each do |gif| %>
  <div>
    <%= image_tag(gif.file_url) %>
  </div>
<% end %>
